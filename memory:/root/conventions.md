# Workflow Conventions

## Commit policy
- **Per-change commits**: every logical change is committed as a separate, atomic commit — no batch commits that mix concerns.
- Each commit has a descriptive message following Conventional Commits format (feat/fix/refactor/chore/docs/test).
- Push after each commit to origin/main.
